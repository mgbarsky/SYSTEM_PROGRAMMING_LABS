#include "../include/fib.h"


void fib (int ** fib_ptr, int n ){	
	//TODO - generate first n elements of the Fibonacci sequence
	//and store them in dynamically allocated int array of size n 
	//whose address is stored in fib_ptr	
	
}