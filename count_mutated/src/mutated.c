#include "../include/mutated.h"

int count_mutated (char *A, int size){	
	//mutate CHANGES some elements of A
	mutate (A, size);
	//TODO implement the counting of mutated characters and return this count
	
	return -1;
}