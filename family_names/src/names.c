#include "../include/names.h"


void combine (char *** combined,  char * f_names,  char * l_names){
	//TODO - complete the function according to the requirements
	
	//first, count how many separators (strchr) - to allocate array of strings into *combined

	
	//now tokenize (strtok) both first names and last names and copy into allocated array	
	
	
}